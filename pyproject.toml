[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "notes"
authors = [{name = "Linux developer's notes", email = "c4rt0gr4ph3r@gmail.com"}]
dynamic = ["version", "description"]
